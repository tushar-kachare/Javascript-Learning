const id = 1038492
let userName = "tushar"
var surname = "kachare"
email = "kach@gmail.com"
let fullName


// Don't use Var  it is function scoped it leaks out when in block like {} this one

// without datatype is very dengerous always global

// let -> block scoped {} eg] if else condition variable, only for that block
console.table([id,userName,surname,email,fullName])
